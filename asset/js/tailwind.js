// Initialization for ES Users
import {
    Collapse,
    Dropdown,
    initTWE,
} from "tw-elements";

import 'tw-elements';
initTWE({ Collapse, Dropdown });
initTWE({ Input }, { allowReinits: true });

// Initialization for ES Users